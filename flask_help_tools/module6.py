def function6():

    # from app import create_app,db
    # from werkzeug.security import generate_password_hash

    # app = create_app()

    # if __name__ == '__main__':
    #     from app.models import User,Shift,Order,OrderItem
    #     with app.app_context():
    #         db.create_all()
    #     Создание админа
    #     with app.app_context():
    #         hashed_password = generate_password_hash('admin')
    #         admin = User(first_name='first_name', last_name='last_name', email='admin@gmail.com', password_hash=hashed_password, role='admin')
    #         db.session.add(admin)
    #         db.session.commit()
    #     app.run(debug=True)

    return "contents of the run.py file"
